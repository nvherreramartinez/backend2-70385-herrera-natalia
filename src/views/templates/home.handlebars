<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://bootswatch.com/5/minty/bootstrap.min.css">
    <title>{{title}}t</title>
</head>
<hero>
<div class="container mt-5">
    <h1>Producto</h1>
    <div class="row">
        {{#each prods.docs}}
        <div class="col-md-4 mb-4">
            <div class="card h-80 shadows-md">
                <div class="card-body">
                    <h5 class="card-title">{{this.title}}</h5>
                    <p class="card-text">{{this.description}}</p>
                    <p class="card-text">${{this.price}}</p>
                    <a href="/api/products/{{this._id}}">Ver Producto</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    <nav aria-label="Page Navigation">
        <ul class="pagination justify-content-center">
            {{#if prods.hasPrevPage}}
            <li class="page-item">
                <a class="page-link" href="?page={{prods.prevPage}}&limit={{prods.limit}}" aria-label="Previous"><span aria-hidden="true">&laquo;</span></a>
            </li>
            {{/if}}
            {{#each prods.pageNumbers}}
            <li class="page-item {{#if this.isCurrent}}active {{/if}}">
                <a class="page-link" href="?page={{this.number}}&limit={{prods.limit}}">{{this.number}}</a>
            </li>
            {{/each}}
            {{#if prods.hasNextPage}}
            <li class="page-item">
                <a class="page-link" href="?page={{prods.nextPage}}&limit={{prods.limit}}" aria-label="Next"><span aria-hidden="true">&raquo;</span></a>
            </li>
            {{/if}}
        </ul>
    </nav>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</hero>
</html>
